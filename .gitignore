# Secrets — never commit
.env
.env.local
.env.*.local

# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg
*.egg-info/
dist/
build/
.eggs/
.venv/
venv/
.uv/

# uv lockfile artifacts
# (uv.lock IS committed; .uv/ cache is not)
.uv/

# Jupyter
.ipynb_checkpoints/
*.ipynb~

# Data and model artifacts — gitignored; use DVC or direct S3 if needed
# Anchored to repo root so it doesn't swallow ml/src/rat_ml/data/ source package.
/data/
ml/artifacts/
evals/results/

# Mypy / Ruff caches
.mypy_cache/
.ruff_cache/

# Pytest
.pytest_cache/
htmlcov/
.coverage
coverage.xml

# JS / Next.js
node_modules/
web/.next/
web/out/
web/.vercel/
*.tsbuildinfo

# macOS
.DS_Store

# IDE
.idea/
.vscode/
*.swp
*.swo

# OpenTelemetry JSONL trace sink (local dev)
traces.jsonl
*.jsonl

# Generated reports
ml/artifacts/data_quality/
